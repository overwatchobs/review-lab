version: '3.8'

services:
  mock-dashboard:
    image: nginx:alpine
    ports:
      - "3000:80"
    volumes:
      - ./mock-dashboard:/usr/share/nginx/html:ro

  demo-service:
    build: ./demo-service
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
